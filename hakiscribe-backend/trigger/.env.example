# Set in the Trigger.dev dashboard (or via `npx trigger.dev@latest deploy` env sync),
# not read from this file directly — kept here for reference.
# Must match Render's BACKEND_INTERNAL_URL / BACKEND_INTERNAL_SECRET.
BACKEND_INTERNAL_URL=https://hakiscribe-backend.onrender.com
BACKEND_INTERNAL_SECRET=match-the-FastAPI-BACKEND_INTERNAL_SECRET
